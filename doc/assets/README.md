# Demo assets

Drop the README media here. The main README references these exact filenames,
so adding a file with the matching name is all that's needed — no edits.

| File | Shows |
|------|-------|
| `hero.gif` | Top-of-README hero clip (the bar refracting over scrolling content). |
| `refraction.gif` | Edge refraction / lensing close-up. |
| `minimize.gif` | Scroll-to-minimize collapsing the capsule. |
| `selection.gif` | Spring-driven selection pill. |
| `search.gif` | Search tab morphing the capsule into a text field. |
| `accessory.gif` | Bottom accessory (mini-player) shelf. |
| `playground.gif` | The settings playground tuning the glass live. |

Tip: keep clips short (3–6 s), loopable, and ≤ ~3 MB so the page stays light.
